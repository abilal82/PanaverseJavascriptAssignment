

// Styles is used in console.log() as a second parameter to style output,to seperate every problem's output in the console.
const styles = [
    'color: white',
    'background:dodgerblue ',
    'font-size: 20px',
    'border: 1px solid blue',
    'padding: 10px',
  ].join(';'); 
  
  const msge_Important = [
    'color:tomato',
    'font-weight:800'
  
  ].join(';');

  const msge_info = [
    'color:darkgreen',
    'font-weight:800'
  
  ].join(';');
  

// Used in Folder => Problem18
const modified = [
    'color: goldenrod',
    'font-weight:800'
  
  ].join(';');
const original = [
  'color:Blue',
  'font:weight:800'
  ].join(';');
  const values = [
    'color:tomato',
    'font:weight:800'
    ].join(';');


  
  